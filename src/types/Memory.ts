export interface Memory {
  id: number;            // ordem/rota
  images: string[];      // caminhos relativos em /public
  caption: string;       // legenda curta
  note: string;          // texto de sentimento
}
